{
	# Set to your email for ACME registration if desired
	# email you@example.com
}

{$SITE_ADDRESS} {
	encode gzip

	handle /api/* {
		reverse_proxy backend:8000
	}

	handle /health {
		reverse_proxy backend:8000
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}

